import React, { FC } from "react";
import transition from "../transition";

const Games: FC = () => {
  return (
    <div>
      <h1 className="text-accent">Games page</h1>
    </div>
  );
};

export default transition(Games);
