import React, { FC } from "react";
import transition from "../transition";

const Tests: FC = () => {
  return (
    <div>
      <h1>Tests page</h1>
    </div>
  );
};

export default transition(Tests);
