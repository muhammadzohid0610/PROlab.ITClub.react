import React, { FC } from "react";
import transition from "../transition";

const AboutUs: FC = () => {
  return (
    <div>
      <h1>AboutUs page</h1>
    </div>
  );
};

export default transition(AboutUs);
